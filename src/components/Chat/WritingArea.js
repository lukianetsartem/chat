import React from 'react';
import { Button } from '../Button/Button';

export const WritingArea = () => {
  return (
    <section className="chat--writing-area">
      <input />
      <Button onClick={() => {}} label="Start" color="primary" />
    </section>
  );
};
